## 개선 이유

현재 MCP, CLAUDE.MD, SKILLS(test만), agent 두개, hooks(코드 재점검 정도)
해당 구조로 사용중이다.

하지만 명확한 근거와 성능 개선을 확인하고 사용하는 것이 아니라 그냥 시키는 대로 하라는 대로 따라 했던 것 같다…

그래서 성능 개선이 얼만큼 되었는지, 토큰 사용량이 얼마나 줄었는지, 그리고 얼마나 아키텍처 적으로 괜찮은지를 모르는 상황이다.

최근에 클로드 코드 강의를 보았다. 강의 내용 중 키워드를 정리해보면 다음과 같다.

### 클로드 코드 실전편

**컨텍스트 관리**

1. **Second brain 구축하기**
    1. 배운 것들을 로컬 마크다운에 저장, 새로운 패턴, 해결책, 의사결정 이유등
2. **자동 메모리 시스템 /memory MEMORY.MD**
3. **Lazy Loading**
    1. [CLAUDE.MD](http://CLAUDE.MD) 에는 각 MD 파일의 참조만 한다.
4. **폴더 별로 따로 [CLAUDE.MD](http://CLAUDE.MD) 를 만든다.**
5. **한 세션에서는 한 feature 만**
    1. 로그인 구현이 끝나면 /clear를 하거나 아에 세션을 새로 시작하는게 낫다.
6. **실전 워크플로우**
    1. Plan Mode에서 전체 설계
    2. /clear 또는 새 세션
    3. 첫 번째 단계만 구현
    4. 완료 → /clear → 다음 단계
7. **부풀어진 컨텍스트보다 신선한 컨텍스트가 좋다**
8. **mcp 토큰 모니터링 /context, /mcp**
    1. 작업에 사용하지 않는 mcp 는 비활성화 할 줄 알아야 한다.
9. **커스텀 mcp**
    1. 자주 쓰는 기능을 모아서 커스텀 mcp 로 만들면 좋다
10. **Mermaid  아키텍처**
    1. 기능별로 MERMAID 아키텍처 MD 파일 정리해두면 좋다.

        ```sql
        클라이언트 → api게이트웨이 -> 인증 서비스
                         |
                     주문 서비스  ->  결제 서비스
                         |   
                      재고 서비스
        ```

11. **무거운 작업은 스크립트로 오프로드**
    1. 클로드 대화 대신 스크립트 작성 → 실행 → 결과만 전달 하여 컨텍스트 오염 방지
    2. db 마이그레이션 검증, 로그 분석, api 응답 비교 등
12. **Plan Mode 4단계**
    1. 작업 설명 → 계획 제시 → 리뷰 & 피드백 → (Accept → 실행)
13. **TDD 기반 스마트 코딩**
    1. 기능 하나 추가
    2. 테스트 돌려서 확인
    3. 린트/포맷 체크
    4. 문제 없으면 커밋
    5. 다음 기능으로 넘어가기
14. **클로드 Thinking 로그 읽기**
    - 잘못된 가정 발견 시 즉시 중단
15. **다른 ai에게 비평받기**
    1. custom skill로 자동화
    2. gemini api 연동
16. **에러 로그를 그대로 붙여 넣어라**
    1. 에러 로그를 그대로 클로드에게 던져라
17. **로컬 [Todo.md](http://Todo.md) 활용**
    1. 하루 시작 - 할 일을 [TODO.md](http://TODO.md) 에 체크리스트로 작성
    2. Claude에게 [TODO.md](http://TODO.md) 읽고 첫번쨰 항목부터 시작해 지시
    3. Agent TEAMS 로 여러 태스크 병렬 처리 가능
    4. 세션 종료 시 “Todo.md” 업데이트 해줘 → 진행 상황 자동 반영

**18. WAT 프레임워크**

- WorkFlows - Agents - Tools
- 작업 흐름을 정리하고
- AI 로 병렬처리 작업을 하고
- AI 한테 어떤 도구들을 줘야 작업을 잘 할것인지

### 클로드 코드 심화편

1. **MCP vs Sub-Agent vs Skills**
    - 컨텍스트 효율 비교 mcp (비용 높음) < Sub-Agent(중간) < Skill(비용 낮음)
2. **Skills**
    - **좋은  description의 3가지 조건**
        1. 핵심 기능 첫 문장
        2. 트리거 표현 3개
        3. 차별(비슷한 다른 스킬과 구별되는가?)
    - skill-creator 플러그인
3. **Sub-Agent**
    - **Sub-Agent & 병렬 실행**
    - 서브 에이전트 장점
        - 독립적인 작업 병렬 분배
        - sub-agent에 명확한 역할과 범위 지정
        - 결과를 메인 에이전트에서 통합 처리
        - 에러 발생 시 gracefule fallback 설계
4.  **내장 Sub-Agent 3종 + 도우미**
5. **커스텀 에이전트 만들기**
    - 에이전트 범위와 실행 방식
6. **에이전트 실전 패턴 4가지**
    - 격리: 독립 브랜치에서 안전하게 작업, 메인 코드에 영향 없이 실행 가능
    - 병렬: 여러 에이전트를 동시에 실행하여 작업 속도를 극대화
    - 체인: 한 에이전트의 결과를 다음 에이전트에 전달하여 순차 처리
    - 재개: 이전 에이전트의 컨텍스트를 이어 받아 작업을 계속 진행
7. **메인 대화 vs Sub-Agent**
    - 직접하기 - 메인 컨텍스트, 심부름시키기 - 서브 에이전트
8. **Hooks**
    - 훅에 시간이 오래 걸리는 작업을 넣으면 클로드 전체가 느려질 수 있다.
    - 타임아웃을 꼭 설정해주고, 무거운 작업은 백그라운드에서
9. **멀티 인스턴스 운영**
    - 터미널 탭을 여러개를 열고, 각 터미널에 클로드를 연다
    - 독립적인 기능을 개발할 때, 병행을 하면 좋다.
    - 터미널마다 이름을 바꿔두는게 좋다.
10. **Git Workflow**
    - commiy history나 리모트 설정은 공유하면서 파일 시스템은 별도로 사용
    - 클로드에는 워크트리 기능이 내장되어 있어서 명령어 한 줄이면 된다.
11. **mcp vs 배쉬 스크립트**
    - mcp 가 편리하긴 하지만 도구 설명만으로 공간을 많이 차지한다.
    - 이럴 때는 Bash 스크립트를 사용하는 걸 추천
    - Bash 스크립트는 사용자가 직접 실행하라고 해야 실행하지만
    - mcp  는 클로드가 알아서 실행
12. **커스텀 mcp 서버**
    - Gemini api 연결해서 plan-review 라는 mcp 생성
13. **/loop**
    - ‘/loop 5m 배포 상태확인해줘’
    - 5분마다 클로드가 알아서 배포 상태를 체크해준다.
14. **통합 데모 - 1차 피드백 한 큐에 수정하기**
    1. 클라이언트 피드백 확인
    2. 플랜 생성
    3. 커스텀 mcp를 활용해 리뷰를 다시 받기
    4. 받은 플랜을 [plan.md](http://plan.md) 파일에 넣어서 참조
    5. 서브 에이전트를 활용해서 병렬 실행
15. **코드 리뷰 플러그인**
- code-review plugin

1. **클라이언트 보고서 SKILL**
- 클라이언트 피드백을 받아서 구현 한 걸, 리포트로 만드는 것

---

간단하게 키워드만 이정도…. 이 내용을 기반으로 현재 구조를 개선 하고, 개선 한 결과를 테스트 해 보려고 한다.
