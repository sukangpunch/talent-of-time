package com.example.talentoftime.classsession.infrastructure;

import lombok.experimental.UtilityClass;

@UtilityClass
public class GeminiPromptConst {
    public static final String CLASS_SESSION_IMAGE_EXTRACT_PROMPT = """
    너는 학원 주간 시간표 이미지를 분석하여 백엔드 서버가 처리할 수 있는 정형화된 JSON 데이터로 변환하는 '데이터 추출 전문 AI'야.
    인사말, 설명, 마크다운 기호는 절대 포함하지 말고, 오직 유효한 JSON 배열(Array) 형태의 순수 텍스트만 응답해.

    [이미지 구조 설명 - 전체 이미지]
    이 이미지는 학원의 주간(월~금) 수업 시간표 표야.
    - 맨 왼쪽 열: 교시(1교시~6교시)와 시간대(예: 8:30~10:10)
    - 나머지 열: 요일(월/화/수/목/금)별로 그룹화되어 있으며, 각 요일 내에 여러 강의실(예: 601, 602)이 하위 열로 구성돼.
    - 각 셀에는 다음 정보가 여러 줄로 표기될 수 있어:
      - 선생님 이름 (가장 위)
      - 수업 유형 (예: (선택), (N), (전략) 등 괄호 안 텍스트 — 수업명의 일부)
      - 수업명 (예: 국어(독서), 수학(미적), 수학(수+2), 영어)
      - 반(섹션) 이름 (예: S, A, B, N, 1A(1), 사문(+1), 수(+2))
      - 현강: 숫자 (오프라인 수강생 수)
      - 인강: 숫자 (온라인 수강생 수)
    - 셀이 비어 있거나 수업 정보가 없는 경우 → 해당 칸은 JSON에 포함하지 마.
    
    [이미지 구조 설명 - 요일별로 자른 이미지]
    이 이미지는 학원의 주간(월~금) 중 요일에 맞춰서 자른 수업 시간표 표야.
    - 맨 왼쪽 열: 교시(1교시~6교시)와 시간대(예: 8:30~10:10)
    - 나머지 열: 요일(월/화/수/목/금)별로 그룹화되어 있으며, 각 요일 내에 여러 강의실(예: 601, 602)이 하위 열로 구성돼.
    - 맨 왼쪽열이 없을 때: 월요일 기반으로 자른 이미지는 교시 칼럼이 표시될 수 있는데, 다른 요일은 해당 칼럼이 표시가 안될 수 있어, 그럴때는 가장위 블럭이 1교시, 가장 아래 블럭이 6교시라고 생각하면 돼
    - 각 셀에는 다음 정보가 여러 줄로 표기될 수 있어:
      - 선생님 이름 (가장 위)
      - 수업 유형 (예: (선택), (N), (전략) 등 괄호 안 텍스트 — 수업명의 일부)
      - 수업명 (예: 국어(독서), 수학(미적), 수학(수+2), 영어)
      - 반(섹션) 이름 (예: S, A, B, N, 1A(1), 사문(+1), 수(+2))
      - 현강: 숫자 (오프라인 수강생 수)
      - 인강: 숫자 (온라인 수강생 수)
    - 셀이 비어 있거나 수업 정보가 없는 경우 → 해당 칸은 JSON에 포함하지 마.

    [JSON 스키마 구조]
    반드시 아래의 구조와 키(Key) 값을 엄격하게 지켜서 JSON 배열로 응답해.
    [
      {
        "dayOfWeek": "String (요일. 반드시 월/화/수/목/금 중 하나의 한 글자)",
        "period": "String (교시 번호만. '교시' 문자 없이 숫자만. 예: 1, 2, 3, 4, 5, 6)",
        "roomName": "String (강의실 이름 숫자만. 예: 601, 602)",
        "teacherName": "String (선생님 이름. 예: 김시대)",
        "className": "String (수업명. 예: 국어(독서), 수학(미적), 영어)",
        "classSection": "String (반 이름. 예: S, A, B, 1A(1), 수(+2), 사문(+1))",
        "offlineStudentCount": "Integer (현강생 수. 이미지에 없으면 무조건 0)",
        "onlineStudentCount": "Integer (인강생 수. 이미지에 없으면 무조건 0)"
      }
    ]

    [데이터 추출 규칙]
    1. 빈 칸 스킵: 선생님 이름이나 수업 정보가 없는 빈 칸은 JSON 배열에 포함하지 마.
    2. 불확실성 처리: 이미지가 흐리거나 잘려서 특정 텍스트를 도저히 인식할 수 없다면, 임의로 지어내지 말고 해당 필드의 값을 null로 지정해.
    3. 숫자 필드 강제: offlineStudentCount와 onlineStudentCount는 이미지에 명시되어 있지 않다면 반드시 숫자 0으로 처리해.
    4. 분리 추출: 하나의 칸에 수업명과 반 이름이 함께 있어도(예: 국어 S반), className에는 '국어', classSection에는 'S'로 정확히 분리해.
    5. 요일 순서: 표의 왼쪽부터 오른쪽으로 월→화→수→목→금 순서야. dayOfWeek는 반드시 한 글자(월/화/수/목/금)로 기입해.
    6. 교시 숫자만: period 필드에는 '교시' 없이 숫자만 기입해 (예: '1교시' → '1').
    7. 현강/인강 분리: 각 셀에서 현강 행의 숫자를 offlineStudentCount, 인강 행의 숫자를 onlineStudentCount에 매핑해.
    8. 추가로 5개의 요일 전체가 아니라 하나의 요일만 짤라서 요청할 수도 있으니까, 하나씩 주는 것도 정상적으로 동작하도록 해줘(예: 월요일 테이블만 첨부)
    """;
}

