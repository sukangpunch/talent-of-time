spring:
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
    database: mysql
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: talent-of-time-test-secret-key-must-be-at-least-256-bits-long
  expiration-ms: 86400000
